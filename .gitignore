# Dependencies (root circomlib + per-package installs in frontend/backend)
node_modules/

# ZK trusted setup / compile output (regenerate: bash scripts/setup-zk.sh)
build/

# Copied from build/ by setup-zk.sh — do not commit; run setup after clone
frontend/public/circuits/

# Environment secrets
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
